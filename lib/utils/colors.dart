import 'package:flutter/material.dart';

class AppColors {
    static const Color appBlueInit = Color.fromRGBO(40, 40, 120, 1.0);
    static const Color welcome = Color.fromRGBO(40, 40, 120, 1.0);
    static const Color appBlueFinish = Color.fromRGBO(30, 40, 144, 1.0);
    static const Color buttonNext = Color.fromRGBO(30, 144, 255, 1.0);
    static const Color opacityColor = Color(0xFF0E3311);
}

class AppOpacity {
    static const double  opacityValue  = 0.50;
}